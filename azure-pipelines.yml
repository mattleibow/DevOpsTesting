pool:
  vmImage: 'windows-2022'

trigger:
  - master

steps:
  - pwsh: .\install-vs.ps1 -Version '17/pre'
    displayName: Install Visual Studio
  - task: PublishBuildArtifacts@1
    inputs:
      artifactName: 'output'
      pathToPublish: 'output'
